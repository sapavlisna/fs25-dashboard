# Runtime data — history snapshots, dashboard_data copies
data/
*.jsonl

# Node
Server/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Build artifacts (set by GitHub Actions release workflow)
dist/
*.zip
!docs/*.zip

# Personal local-override files (deploy paths, server config, etc.)
*.local.bat
*.local.json
*.local.ps1
.local/
log.txt

# Cloud deploy secrets — NEVER commit real API keys / passwords.
# Only .env.example (the placeholder template) lives in git.
.env
**/.env
!.env.example
!**/.env.example

# Playwright smoke test runtime artefakty
Server/test-results/
Server/playwright-report/
Server/.tmp/
**/.tmp/
