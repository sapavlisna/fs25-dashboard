# Sklady/sila (`storage`)

- **Soubory:** index.html (donutSVG donut-warn>=70 / donut-full>=95 + silo/storage render, množství/kapacita, owner) + bell.js (STORAGE_HI)
- **Počet stavů:** 20
- **biggestRisk:** bar-fill warn/full prahy, donut-warn/donut-full, bell storage-full, flash-up/flash-down — vše nyní pokryto (storage.spec.js). Zbývající nepokrytý stav: sklad skrytý uživatelem (hidden zone) — blokováno chybějícím test-setup endpointem pro seedování 'tt:storages:hidden' v localStorage (test.fixme v storage.spec.js:325).

> Povrch: Sklady/sila (sec-storage + bell storage-full)

| stav | trigger | podmínka (file:line) | DOM (selektor+třída) | křížové efekty | pokrytí |
|---|---|---|---|---|---|
| sklad prázdný – žádné sklady v payloadu | storage | storage.length === 0 (index.html:1765) | #storage-body tr td.empty — empty (text: Žádné sklady.) | cnt-storage = 0 | scenarioEmptyFarm |
| sklad skrytý uživatelem – sklad v hidden zone | storage[].storageName · storage[].type | TableTools.getHidden('storages').has(key) where key = `${type}:${storageName}` (index.html:1776) | #storages-hidden-body — hidden-row (row moves to storages-hidden-body) | cnt-stor-hidden count increments | — |
| sklad sbalený (collapsed) – group-item hidden-row | storage[].storageName | collapsedGroups.has(key) === true (default: storageDefaultCollapsed === true) (index.html:1692) | tr.group-item[data-group] — hidden-row (display:none); group-toggle textContent = '▶' | relayoutMasonry() triggered on toggle click | section-cfg.spec.js (storageExpanded toggle) |
| sklad rozbalený (expanded=false, default) – normální tabulkové řádky s barem | storage[].items[].amount · storage[].items[].capacity | storageExpanded === false (DashState default), item visible (index.html:1729) | tr.group-item td div.bar-wrap div.bar-fill — '' (žádná class – OK stav, 21–69 %) | — | scenarioHarvestReady, scenarioLowFuel, scenarioPlan3Years, scenarioMultiFruitTypes, scenarioLayoutStress |
| bar-fill warn – položka skladu 70–94 % | storage[].items[].amount · storage[].items[].capacity | ipct >= 70 && ipct < 95 where ipct = Math.round(amount / capacity * 100) (index.html:1731) | tr.group-item td div.bar-bg div.bar-fill — warn (background: var(--accent2), žlutá) | donut-warn na hlavičce skladu pokud agregát >= 70 % | storage.spec.js:92 (storage-warn-boundary-ok) |
| bar-fill full – položka skladu >= 95 % | storage[].items[].amount · storage[].items[].capacity | ipct >= 95 (index.html:1731) | tr.group-item td div.bar-bg div.bar-fill — full (background: var(--teal), tyrkysová) | donut-full na hlavičce skladu · bell: storage-full alert (severity info) | storage.spec.js:136 (storage-full-boundary-ok) |
| donut-warn – silo header, agregát 70–94 % | storage[].items[].amount · storage[].items[].capacity | pct >= 70 && pct < 95 where pct = Math.round(totalAmount / totalCap * 100) (index.html:328) | tr.st-row svg.silo-donut — donut-warn (color: var(--accent2)) | — | storage.spec.js:92 (storage-warn-boundary-ok) |
| donut-full – silo header, agregát >= 95 % | storage[].items[].amount · storage[].items[].capacity | pct >= 95 (index.html:328) | tr.st-row svg.silo-donut — donut-full (color: var(--red)) | bell: storage-full alert | storage.spec.js:136 (storage-full-boundary-ok) |
| donut absent – totalCap == 0 (neznámá kapacita) | storage[].items[].capacity | totalCap <= 0 (index.html:1699) | tr.st-row td — žádný donut SVG (donut = '') | silo-pct text absent | — |
| silo prázdné – group-meta text 'prázdné' | storage[].items | items.length === 0 (nebo hideEmpty filtruje vše na 0) (index.html:1703) | tr.st-row .group-meta — text: 'prázdné' | — | scenarioEmptyFarm (storage: [{ storageName:..., items: [] }]) |
| storageHideEmpty – prázdné položky filtrované | storage[].items[].amount | DashState.get('storageHideEmpty') === true && item.amount <= 0 (index.html:1695) | tr.group-item (absentní) — řádek se nevykreslí vůbec | totalAmount / totalCap / pct přepočítány bez těchto položek | section-cfg.spec.js (flipCfg storageHideEmpty) |
| storageShowCapacity=false – kapacita skryta | storage[].items[].capacity | DashState.get('storageShowCapacity') === false (index.html:1740) | tr.group-item td:nth-child(3) — text: '—' (místo fmt(capacity)) | — | section-cfg.spec.js (flipCfg storageShowCapacity) |
| storageShowBar=false – bar skrytý, jen % text | storage[].items[].amount · storage[].items[].capacity | DashState.get('storageShowBar') === false (index.html:1734) | tr.group-item td div.bar-wrap — bar-bg + bar-fill absent; jen <span class=bar-pct> | — | storage.spec.js:225 (storage-show-bar-off) |
| storageShowPercent=false – % text skrytý | storage[].items[].amount · storage[].items[].capacity | DashState.get('storageShowPercent') === false (index.html:1735) | tr.group-item td .bar-pct — absent | — | storage.spec.js:243 (storage-show-percent-off) |
| storageExpanded=true – 2-sloupcové st-items tělo s per-item % (ipct) | storage[].items[].amount · storage[].items[].capacity | DashState.get('storageExpanded') === true (index.html:1717) | tr.st-body-row .st-items .st-item — st-item (+ flash-up / flash-down třídy z getFlashCls) | sekce dostane CSS třídu expanded-storage | section-cfg.spec.js (storage expanded renders the 2-column item body) |
| storageExpanded=true + >= 2 komodity – stacked přehledový bar | storage[].items[].amount · storage[].items[].capacity | storageExpanded === true && segCount >= 2 && totalCap > 0 (index.html:1710) | tr.st-row .st-stack — st-stack (flex segmenty s colorForCommodity barvami + st-seg-free šedý segment) | — | storage.spec.js:283 (storage-expanded-multi) |
| flash-up silo header – celkové množství v silu vzrostlo | storage[].items[].amount | noteFlashRows: total > prev_total (10 s flash okno, nepřepíše aktivní flash) (index.html:1706) | tr.st-row.collapsible — flash-up (td background: accent 35 %) | isFlashEnabled('storage','header') musí být true (DashState flashEnabled.storage + flashEnabled.storage::header) | storage.spec.js:355 (storage-flash-up-header) |
| flash-down silo header – celkové množství kleslo | storage[].items[].amount | noteFlashRows: total < prev_total (index.html:1706) | tr.st-row.collapsible — flash-down (td background: rgba(224,82,82,0.35)) | — | storage.spec.js:368 (storage-flash-down-header) |
| flash-up / flash-down na položce skladu | storage[].items[].amount | noteFlashRows item-level: item.amount changed vs prev tick (key: item::${flashKey}::${item.name}) (index.html:1723) | tr.group-item (compact) nebo .st-item (expanded) — flash-up / flash-down (tr override: td background; .st-item override: background-color) | flash-channel 'item' ovládán DashState flashEnabled['storage::item'] | storage.spec.js:380 (storage-flash-item-level) |
| bell storage-full – alespoň jedna položka skladu >= 95 % | storage[].items[].amount · storage[].items[].capacity | item.capacity > 0 && item.amount / item.capacity >= 0.95 (STORAGE_HI = 95) (bell.js:131) | #bell-btn, #bell-count, #bell-list li.bell-info — has-alerts na bell-btn; bell-count visible; li.bell-item.bell-info s key='storage-full' | target='sec-storage' – klik scrolluje na sekci + card-flash · fingerprint zahrnuje storageName+': '+itemName – re-fires jen pokud se změní set postižených skladů | storage.spec.js:136 (storage-full-boundary-ok), storage.spec.js:402 (storage-bell-full-multi) |
